numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.65

# Only base_predictor_training.py needs torch: it trains and calibrates the
# base model and then writes the evaluation split's calibrated log-posterior to
# eval_log_post.npz. rejopt_eval.py reads that file and is pure NumPy/SciPy --
# the inference is exact and closed-form, so there is nothing to put on a GPU.
#
# On a CUDA machine, install the wheel matching the driver, e.g.
#   --extra-index-url https://download.pytorch.org/whl/cu126
torch>=2.2
torchvision>=0.17
