# Starter code for "Know what you don't know under a new prior".
#
# The competition publishes the base predictor's calibrated posteriors, not the
# images, so nothing here needs a deep-learning framework.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7       # only for evaluate.py --plot
